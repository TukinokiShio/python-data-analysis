# Python 数据分析课程项目记忆

## 项目用途

本工作区用于完成 Python 数据分析课程的作业、实验和相关学习记录。

## 工作约定

- 每次完成一个 Python 数据分析作业后，检查结果并创建一次清晰的 Git commit。
- 每次作业 commit 完成后，立即 push 到 GitHub 的 `origin` 远程仓库。
- 作业文件、数据说明、分析代码和必要的运行说明应一并纳入版本控制；临时缓存、解释器环境、密钥和大体积生成文件不得提交。
- 保留已有作业内容和用户修改，不使用破坏性 Git 操作覆盖未提交工作。
- 提交前至少进行与作业规模匹配的运行验证，并在提交信息或 README 中说明必要的运行方式。

## 默认提交流程

1. 检查 `git status`，确认只包含本次作业相关变更。
2. 运行作业脚本或 notebook 中可复现的关键代码，确认无明显错误。
3. 使用描述性 commit message 提交。
4. push 到 `origin`，并确认远程分支已更新。

