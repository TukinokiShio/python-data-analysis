# Python 数据分析第二周作业

## 说明

附件中的 `.md` 和图片是本次作业的题目要求；本文件是对应的作答内容，第三题的可复现代码见 [carseats_analysis.py](carseats_analysis.py)。

## 1. 一元线性回归中的残差性质

设带截距的一元线性回归模型为

$$y_i=\hat\beta_0+\hat\beta_1x_i+e_i,$$

写成矩阵形式为 $y=X\hat\beta+e$，其中 $X=[\mathbf 1,x]$。普通最小二乘法使残差平方和

$$RSS=e^Te=(y-X\hat\beta)^T(y-X\hat\beta)$$

最小。对 $\beta$ 求导并令其为零，得到 OLS 正规方程：

$$X^T(y-X\hat\beta)=X^Te=0.$$

由于 $X$ 的第一列是全 1 向量，$X^Te=0$ 的第一行就是

$$\mathbf 1^Te=\sum_{i=1}^n e_i=0.$$

而第二行给出

$$x^Te=\sum_{i=1}^n x_i e_i=0.$$

因此，在模型含有截距且 OLS 解存在时，残差和为零，残差与自变量的内积也为零。这里的“正交”是样本向量意义下的正交，并不等同于残差与随机变量在总体意义下独立。若回归模型不含截距，上述第一条性质一般不成立。

## 2. 加入随机无关特征后 $R^2$ 与调整后 $R^2$ 的变化

在原模型中加入一个新特征后，新模型包含原模型作为特例：只需把新特征的系数设为 0，就能得到原来的拟合结果。因此，最小化后的残差平方和满足

$$RSS_{new}\le RSS_{old}.$$

在同一个响应变量和同一个样本上，$TSS=\sum_i(y_i-\bar y)^2$ 不变，所以

$$R^2=1-\frac{RSS}{TSS}$$

只能上升或保持不变。即使新特征在总体上与 Sales 无关，在有限样本中也可能与响应变量产生偶然相关，从而使样本内 RSS 降低、$R^2$ 上升；这不表示该特征具有真实预测价值。

调整后 $R^2$ 会惩罚参数数量：

$$\bar R^2=1-\frac{RSS/(n-p-1)}{TSS/(n-1)},$$

其中 $n$ 是样本量，$p$ 是解释变量个数。加入一个变量后，RSS 的下降必须足够大，才能抵消自由度从 $n-p-1$ 变为 $n-p-2$ 带来的惩罚。因此，$\bar R^2$ 可能下降，也可能在新变量带来足够额外解释力时上升；若新变量只是随机噪声，通常更容易出现下降。

## 3. Carseats 多元线性回归

### 3.1 数据与模型

Carseats 数据包含 400 家门店。因变量 `Sales` 表示销量（千件），解释变量为 `Price`、`Income`、`Advertising` 以及分类变量 `ShelveLoc`。模型写为：

$$Sales=\beta_0+\beta_1Price+\beta_2Income+\beta_3Advertising+\beta_4I(ShelveLoc=Medium)+\beta_5I(ShelveLoc=Good)+\varepsilon.$$

代码中显式指定 `ShelveLoc=Bad` 为基准组，因此分类变量的两个系数分别表示 `Medium` 相对 `Bad`、`Good` 相对 `Bad` 的差异。数据字段含义与样本量依据 ISLP 的 Carseats 数据说明。[ISLP Carseats 数据说明](https://intro-stat-learning.github.io/ISLP/datasets/Carseats.html)

### 3.2 模型拟合报告

运行 `carseats_analysis.py` 后，完整的 statsmodels 输出会保存为 [carseats_model_summary.txt](carseats_model_summary.txt)，系数表保存为 [carseats_coefficients.csv](carseats_coefficients.csv)。本次运行的关键结果如下：

| 项目 | 数值 |
|---|---:|
| 样本量 | 400 |
| $R^2$ | 0.627 |
| 调整后 $R^2$ | 0.622 |
| F 统计量 | 132.4 |
| F 检验 p 值 | $4.89\times10^{-82}$ |

| 变量 | 系数 | p 值 | 解释 |
|---|---:|---:|---|
| 截距 | 10.3916 | <0.001 | 仅作为模型基准项解释 |
| Price | -0.0570 | <0.001 | 价格每增加 1 个单位，预测销量平均减少 0.057 千件 |
| Income | 0.0137 | <0.001 | 社区收入每增加 1 千美元，预测销量平均增加 0.0137 千件 |
| Advertising | 0.1056 | <0.001 | 广告投入每增加 1 千美元，预测销量平均增加 0.1056 千件 |
| ShelveLoc[Medium] | 1.8959 | <0.001 | 相对 Bad，预测销量平均高 1.8959 千件 |
| ShelveLoc[Good] | 4.8359 | <0.001 | 相对 Bad，预测销量平均高 4.8359 千件 |

在控制其他变量后，`Price` 与销量呈负相关，`Income`、`Advertising` 以及更好的货架位置与销量呈正相关。模型的整体 F 检验显著，但 $R^2=0.627$ 也说明仍有约 37.3% 的样本内销量波动没有被本模型解释。

### 3.3 ShelveLoc 基准组与商业含义

基准组是 `Bad`。因此，在 `Price`、`Income` 和 `Advertising` 相同的条件下，`ShelveLoc[Good]` 的系数表示货架位置从 `Bad` 改为 `Good` 后的平均销量差异，单位是“千件”。如果该系数为正，说明更好的货架陈列与更高销量相关；例如系数为 4，表示模型预测的销量平均高 4 千件。这个结论是控制模型中其他变量后的条件相关关系，不应直接表述为货架位置单独造成了全部增量销量，因为模型仍可能遗漏门店规模、竞争环境或促销执行等因素。

### 3.4 VIF 与多重共线性

代码对数值变量和两个 ShelveLoc 哑变量计算 VIF，且不把截距列作为诊断对象。结果会保存为 [carseats_vif.csv](carseats_vif.csv)。通常可用 VIF=5（或更严格的 10）作为需要进一步检查的经验阈值；阈值不是数学定理，应结合系数稳定性、标准误和业务含义判断。若本次所有 VIF 均明显低于 5，则没有明显的多重共线性风险；这只说明所选解释变量之间的线性相关性不强，不代表模型已经满足正态性、同方差性或因果识别等其他假设。

本次 VIF 结果为：

| 变量 | VIF |
|---|---:|
| ShelveLoc[Medium] | 1.4938 |
| ShelveLoc[Good] | 1.4973 |
| Price | 1.0077 |
| Income | 1.0124 |
| Advertising | 1.0094 |

所有 VIF 都接近 1 且明显低于 5，因此没有发现明显的多重共线性风险。

## 运行方式

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe carseats_analysis.py
```

脚本优先读取仓库根目录的 `Carseats.csv`；若该文件不存在，会从脚本中记录的公开地址读取同一数据集。
